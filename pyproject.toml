[tool.black]
line-length = 120
target-version = ['py311']

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 120
target-version = 'py311'

[tool.ruff.per-file-ignores]
"bannou/database/migrations/env.py" = ["F401"]
"bannou/database/models/__init__.py" = ["F401"]